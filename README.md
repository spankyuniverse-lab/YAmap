# YAmap — Парсер Яндекс.Карт

Парсер организаций с Яндекс.Карт с поддержкой динамической подгрузки (infinite scroll) и парсинга по категориям (аналог 2ГИС).

## Установка

```bash
pip install -r requirements.txt
playwright install chromium
```

Для разработки (тесты):

```bash
pip install -r requirements-dev.txt
```

## Использование

### По поисковому запросу

```bash
python yandex_parser.py "кофейни Москва"
python yandex_parser.py "автосервис Казань" -n 200 -o авто.csv
python yandex_parser.py "аптеки Москва" --api-intercept
python yandex_parser.py "рестораны СПб" --detail

# Аналогично через модуль
python -m yamap "кофейни Москва"
```

### По категориям (аналог 2ГИС)

```bash
# Посмотреть каталог категорий
python yandex_parser.py --list-categories

# Все подкатегории группы «еда» (рестораны, кафе, бары, пиццерии, ...)
python yandex_parser.py --city Москва --category еда

# Конкретные категории
python yandex_parser.py --city Москва --category рестораны кафе бары

# Несколько групп + отдельные категории
python yandex_parser.py --city СПб --category авто красота шиномонтаж

# Все категории каталога сразу
python yandex_parser.py --city Казань --all-categories -n 100

# С API-перехватом и обогащением карточек
python yandex_parser.py --city Москва --category здоровье --api-intercept --detail
```

Результат по категориям сохраняется в Excel:
- **Лист «Все результаты»** — сводная таблица с колонкой «Поисковый запрос»
- **Отдельный лист на каждую категорию** (рестораны, кафе, бары, …)

## Параметры

| Параметр | Описание |
|---|---|
| `query` | Поисковый запрос (режим одиночного поиска) |
| `--city` | Город для парсинга по категориям |
| `--category` | Категории или группы (еда, авто, рестораны, кафе, …) |
| `--all-categories` | Парсить все категории из каталога |
| `--list-categories` | Показать каталог категорий и выйти |
| `-n, --max-results` | Максимум организаций на запрос/категорию (по умолчанию 500) |
| `-o, --output` | Файл: `.xlsx` или `.csv` |
| `--api-intercept` | Перехватывать JSON из внутреннего API (надёжнее DOM) |
| `--detail` | Открывать карточку каждой организации для телефона/сайта |
| `--no-headless` | Показывать браузер |
| `--scroll-pause` | Пауза между прокрутками, сек (по умолчанию 1.0) |
| `--detect-selectors` | Автодетект CSS-селекторов на живой странице (сохраняет в кеш) |
| `--show-selectors` | Показать текущие активные селекторы |
| `--reset-selectors` | Удалить кеш и вернуться к hardcoded-селекторам |

## Каталог категорий

| Группа | Подкатегории |
|---|---|
| **еда** | рестораны, кафе, бары, пиццерии, суши-бары, столовые, фастфуд, кофейни, кондитерские, пекарни, шаурма, бургерные, доставка еды |
| **продукты** | продуктовые магазины, супермаркеты, мясные, рыбные, овощи и фрукты, молочные, алкогольные |
| **здоровье** | аптеки, больницы, поликлиники, стоматологии, медцентры, ветклиники, лаборатории, оптика, косметология |
| **авто** | автосервисы, шиномонтаж, автомойки, автозапчасти, АЗС, автосалоны, эвакуаторы, парковки |
| **красота** | салоны красоты, парикмахерские, барбершопы, маникюр, массаж, спа, солярии, тату |
| **покупки** | ТЦ, одежда, обувь, электроника, мебель, стройматериалы, цветы, зоомагазины, книги, подарки, ювелирные |
| **услуги** | банки, банкоматы, нотариусы, юристы, страховые, фотостудии, ателье, химчистки, ремонт телефонов |
| **образование** | школы, детсады, университеты, колледжи, языковые курсы, автошколы, репетиторы |
| **спорт** | фитнес, тренажёрные залы, бассейны, спорттовары, йога, танцы, бокс, теннис |
| **развлечения** | кинотеатры, театры, музеи, парки развлечений, боулинг, бильярд, караоке, квесты |
| **туризм** | гостиницы, хостелы, турагентства, достопримечательности, аренда авто |
| **транспорт** | такси, каршеринг, автовокзалы, ж/д вокзалы, аэропорты, грузоперевозки |
| **недвижимость** | агентства, новостройки, управляющие компании, ЖК |
| **дом** | мебель, сантехника, электрика, окна, кухни, потолки, кондиционеры |
| **IT** | компьютеры, ремонт ПК, IT-компании, провайдеры, веб-студии |

## Собираемые данные

- Название
- Адрес
- Телефон (с `--detail` или `--api-intercept`)
- Сайт (с `--detail` или `--api-intercept`)
- Рейтинг
- Количество отзывов
- Категория
- Часы работы
- Поисковый запрос (в режиме категорий)
- Ссылка на Яндекс.Карты

## Режимы сбора данных

### DOM-парсинг (по умолчанию)
Скроллит панель результатов, парсит HTML-сниппеты. Использует устойчивые селекторы `[class*="..."]` для защиты от обфускации классов Яндексом.

### API-перехват (`--api-intercept`)
Перехватывает XHR-ответы внутреннего API Яндекс.Карт при скролле. Данные приходят в JSON — надёжнее и полнее, чем DOM-парсинг.

## Автодетект селекторов (F12-анализ)

Яндекс периодически меняет CSS-классы. Парсер умеет автоматически обнаруживать актуальные селекторы через анализ живой страницы.

### Как это работает

1. **При каждом запуске** парсер проверяет, работают ли текущие селекторы
2. Если **кешированные** (из `selectors_cache.json`) работают — использует их
3. Если нет — пробует **hardcoded** селекторы
4. Если и они не работают — запускает **JS-автодетект**: анализирует DOM страницы через эвристики (повторяющиеся элементы, schema.org microdata, ARIA-атрибуты, паттерны текста)
5. Найденные селекторы **кешируются** в `selectors_cache.json`

### Ручной запуск детекции

```bash
# Автодетект — открывает Я.Карты, находит селекторы, сохраняет в кеш
python yandex_parser.py --detect-selectors

# С видимым браузером (для отладки)
python yandex_parser.py --detect-selectors --no-headless

# С конкретным запросом
python yandex_parser.py --detect-selectors "аптеки Москва"

# Показать текущие селекторы
python yandex_parser.py --show-selectors

# Сбросить кеш (вернуться к hardcoded)
python yandex_parser.py --reset-selectors
```

### Что детектится

| Группа | Селекторы |
|---|---|
| **Список** | контейнер прокрутки, карточка результата, ссылка, кнопка «ещё» |
| **Сниппет** | название, адрес, категория, рейтинг, отзывы, часы работы |
| **Карточка** | название, адрес, телефон, сайт, рейтинг, отзывы, часы, категория |

### Эвристики детекции

- **Повторяющиеся элементы**: >=3 однотипных элемента с одинаковым классом = список результатов
- **Schema.org microdata**: `itemprop="openingHours"`, `itemtype="LocalBusiness"`
- **Семантические a[href]**: `tel:` = телефон, внешние ссылки = сайт
- **Визуальные**: самый крупный текст в карточке = название
- **Паттерны классов**: удаление обфусцирующих префиксов (`_1a2b3c_`) для матчинга

## Структура пакета

```
yamap/
├── __init__.py        # публичный API: Organization, run_parser, …
├── __main__.py        # python -m yamap
├── _playwright.py     # patchright/playwright shim
├── models.py          # Organization, dedup
├── proxy.py           # ProxyRotator
├── categories.py      # CATEGORIES + resolve_categories
├── config.py          # YAML/JSON config loader
├── captcha.py         # детект + handle CAPTCHA, bezier-мышь
├── selectors.py       # SelectorEngine + JS auto-detect
├── throttle.py        # AdaptiveThrottle
├── browser.py         # persistent context, warmup, type_like_human
├── scraper.py         # search + scroll + parse + enrich + API
├── output.py          # save_csv/json/xlsx, print_stats
├── resume.py          # ResumeManager
├── runner.py          # run_parser, run_category_parser
├── cli.py             # argparse + interactive_menu
├── validators.py      # normalize_phone/website/email
├── metrics.py         # RunMetrics
└── logging_utils.py   # setup_logging с ротацией

yandex_parser.py        # шим-точка входа (для python yandex_parser.py …)
tests/                  # 246 unit-тестов pytest
```

## Тесты

Полная сюита покрывает чистую логику и парсинг через моки:

```bash
python -m pytest          # все 246 тестов, ~0.7 сек
python -m pytest -v       # с именами тестов
python -m pytest tests/test_validators.py  # один модуль
```

Покрытие: модели и dedup, валидаторы phone/website/email, ротация прокси,
адаптивный троттлинг, метрики, логирование с ротацией, save/load
CSV/JSON/XLSX, ResumeManager, кеш селекторов, парсинг API-ответов,
извлечение координат из URL, CLI argparse, mock Playwright Page.
